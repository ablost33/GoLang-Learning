package crypto

import "testing"

func TestGetCryptoInfo(t *testing.T) {
	t.Parallel()

	tests := []struct {
		name string
	}{{}}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {

		})
	}
}
